# VEKTOR service identity. Replace these values before enabling the agent.
# The matching authorization policy scopes are enforced for every request.
VEKTOR_FLEET_ID=replace-with-fleet-id
VEKTOR_WORKLOAD_ID=replace-with-workload-id
