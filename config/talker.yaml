# Self-test policy for: ros2 run demo_nodes_cpp talker
schema_version: 1
robot_id: talker-demo
discovery_timeout_ms: 750

required_nodes:
  - /talker

required_topics:
  - name: /chatter
    min_frequency_hz: 1.0
    max_frequency_hz: 5.0
    sample_window_ms: 2000
    reliability: reliable
    qos_depth: 10
